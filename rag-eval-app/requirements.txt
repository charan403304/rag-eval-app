# Core (required, no network calls)
numpy>=1.26,<3

# Frontend
streamlit>=1.35

# Real LLM/embedding providers (optional -- offline "hash"/"mock" providers
# in src/config.py need none of these). Uncomment what you use.
anthropic>=0.34
# openai>=1.40
# voyageai>=0.2

# Dev / testing
pytest>=8.0
python-dotenv>=1.0
